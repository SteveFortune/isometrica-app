
/**
 * @note The isCallTree attribute is quite redundant. If we wanted to find all
 * 		 contacts that are in a call tree we should be able to do a joining
 * 		 operation of some sort to query contacts that aren't embedded in users'
 *		 call trees. Not figured out how to do this yet though.
 */
module.exports = function(Contact) {

};
